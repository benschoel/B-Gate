const Process = require("./Process");

//WORKING Deutsch-Oracle
// const main = new Process();
// main.step([{ type: "PAULIX", bits: [0, 1] }]);
// main.step([{ type: "H", bits: [0, 1] }]);
// main.step([{ type: "BLACKBOX", bits: [0, 1] }]);
// main.step([{ type: "H", bits: [0, 1] }]);
// main.step([{ type: "MEASURE", bits: [0, 1, 2, 3] }]);
// main.process();
// const results = main.getResults();
// console.log(results.join(""));
